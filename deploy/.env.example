# 生产环境变量示例 —— 复制为 .env 并填写，绝不提交到 git
POSTGRES_PASSWORD=change-me-strong-password
COOKIE_SECRET=change-me-random-64-chars

# 邮件（接入真实发送时改为 qq，并填写 16 位 SMTP 授权码）
MAIL_PROVIDER=console
QQ_SMTP_HOST=smtp.qq.com
QQ_SMTP_PORT=465
QQ_SMTP_USER=
QQ_SMTP_PASS=
