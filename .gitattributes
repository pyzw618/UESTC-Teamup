# 统一行尾：仓库内一律存 LF，检出行为由 git 自动处理
* text=auto

# 必须在 Linux 侧保持 LF 的文件（容器内执行 / 被镜像 COPY）
*.sh text eol=lf
Dockerfile* text eol=lf
*.yml text eol=lf
*.yaml text eol=lf

# 二进制类型，不做行尾与 diff 处理
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.pdf binary
*.woff binary
*.woff2 binary
*.ttf binary
*.eot binary
